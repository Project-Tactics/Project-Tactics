name: Windows - Debug Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    uses: ./.github/workflows/reusable-windows-build.yml
    with:
      configuration: release
